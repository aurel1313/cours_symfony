{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
    <div class="col-lg-4 col-md-6 mb-4">

        <div class="card">
            <div class="card-body">
                <i class="fa-solid fa-house"></i>
                <h1>{{ bien.nom }}</h1>
                <p>{{ bien.description }}</p>
                <p>Disponible le: {{ bien.dateDispo|date }}</p>
            </div>
        </div>
    </div>


{% endblock %}